name: aws-ecs-delete-all-scheduled-tasks
version: 0.0.4
description: Delete all scheduled tasks related to an app from AWS ECS
keywords:
  - aws
  - ecs
  - scheduled task
properties:
  # Step 1: Configuring AWS
  key:
    type: string
    required: true
  secret:
    type: string
    required: true
  region:
    type: string
    required: false

  # Step 2: Defining event rule
  app-name:
    type: string
    required: true
  